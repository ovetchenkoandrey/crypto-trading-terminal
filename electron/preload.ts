/**
 * Preload script — runs in an isolated context that has access to both the renderer
 * DOM and a subset of Node APIs. Use contextBridge here to expose a safe API to React.
 *
 * Empty for now — will expose Bybit IPC calls in later stages.
 */
export {};
